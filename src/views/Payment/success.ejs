<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kết quả thanh toán</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    
    .result-card {
      background: white;
      border-radius: 15px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.1);
      padding: 40px;
      margin-top: 100px;
      max-width: 600px;
    }
    
    .success-icon {
      color: #28a745;
      font-size: 4rem;
      margin-bottom: 20px;
    }
    
    .error-icon {
      color: #dc3545;
      font-size: 4rem;
      margin-bottom: 20px;
    }
    
    .order-info {
      background: #f8f9fa;
      border-radius: 10px;
      padding: 20px;
      margin: 20px 0;
    }
    
    .btn-custom {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border: none;
      border-radius: 25px;
      color: white;
      font-weight: 600;
      padding: 12px 30px;
      transition: transform 0.3s ease;
    }
    
    .btn-custom:hover {
      transform: translateY(-2px);
      color: white;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <!-- Payment Success Page -->
        <div id="success-page" class="result-card text-center" style="display: none;">
          <i class="fas fa-check-circle success-icon"></i>
          <h2 class="text-success mb-3">Thanh toán thành công!</h2>
          <p class="text-muted mb-4">Cảm ơn bạn đã đặt phòng. Chúng tôi đã nhận được thanh toán của bạn.</p>
          
          <div class="order-info text-start">
            <h5 class="mb-3"><i class="fas fa-receipt me-2"></i>Thông tin đơn hàng</h5>
            <div class="row">
              <div class="col-6">
                <strong>Mã đơn hàng:</strong>
              </div>
              <div class="col-6" id="success-order-id">
                -
              </div>
            </div>
            <div class="row mt-2">
              <div class="col-6">
                <strong>Mã giao dịch:</strong>
              </div>
              <div class="col-6" id="success-transaction-id">
                -
              </div>
            </div>
            <div class="row mt-2">
              <div class="col-6">
                <strong>Thời gian:</strong>
              </div>
              <div class="col-6" id="success-time">
                -
              </div>
            </div>
          </div>
          
          <div class="alert alert-success mt-4">
            <h6 class="alert-heading">
              <i class="fas fa-info-circle me-2"></i>
              Thông tin quan trọng:
            </h6>
            <ul class="mb-0 small text-start">
              <li>Email xác nhận đã được gửi đến địa chỉ email của bạn</li>
              <li>Vui lòng có mặt đúng giờ check-in</li>
              <li>Mang theo giấy tờ tùy thân hợp lệ</li>
              <li>Liên hệ hotline nếu cần hỗ trợ: 1900-xxx-xxx</li>
            </ul>
          </div>
          
          <div class="mt-4">
            <button type="button" class="btn btn-custom me-3" onclick="window.location.href='/'">
              <i class="fas fa-home me-2"></i>
              Về trang chủ
            </button>
            <button type="button" class="btn btn-outline-secondary" onclick="window.print()">
              <i class="fas fa-print me-2"></i>
              In hóa đơn
            </button>
          </div>
        </div>

        <!-- Payment Failed Page -->
        <div id="failed-page" class="result-card text-center" style="display: none;">
          <i class="fas fa-times-circle error-icon"></i>
          <h2 class="text-danger mb-3">Thanh toán thất bại!</h2>
          <p class="text-muted mb-4">Rất tiếc, giao dịch của bạn không thể hoàn tất.</p>
          
          <div class="order-info text-start">
            <h5 class="mb-3"><i class="fas fa-exclamation-triangle me-2"></i>Chi tiết lỗi</h5>
            <div class="row">
              <div class="col-6">
                <strong>Mã đơn hàng:</strong>
              </div>
              <div class="col-6" id="failed-order-id">
                -
              </div>
            </div>
            <div class="row mt-2">
              <div class="col-6">
                <strong>Lỗi:</strong>
              </div>
              <div class="col-6" id="failed-error">
                -
              </div>
            </div>
            <div class="row mt-2">
              <div class="col-6">
                <strong>Thời gian:</strong>
              </div>
              <div class="col-6" id="failed-time">
                -
              </div>
            </div>
          </div>
          
          <div class="alert alert-warning mt-4">
            <h6 class="alert-heading">
              <i class="fas fa-lightbulb me-2"></i>
              Gợi ý khắc phục:
            </h6>
            <ul class="mb-0 small text-start">
              <li>Kiểm tra lại thông tin thẻ/tài khoản</li>
              <li>Đảm bảo tài khoản có đủ số dư</li>
              <li>Thử lại với phương thức thanh toán khác</li>
              <li>Liên hệ ngân hàng nếu vấn đề liên tục xảy ra</li>
            </ul>
          </div>
          
          <div class="mt-4">
            <button type="button" class="btn btn-custom me-3" onclick="window.location.href='/checkout'">
              <i class="fas fa-redo me-2"></i>
              Thử lại
            </button>
            <button type="button" class="btn btn-outline-secondary" onclick="window.location.href='/'">
              <i class="fas fa-home me-2"></i>
              Về trang chủ
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const urlParams = new URLSearchParams(window.location.search);
      const currentPath = window.location.pathname;
      
      if (currentPath.includes('payment-success')) {
        showSuccessPage(urlParams);
      } else if (currentPath.includes('payment-failed')) {
        showFailedPage(urlParams);
      }
    });
    
    function showSuccessPage(urlParams) {
      const orderId = urlParams.get('order_id');
      const transactionId = urlParams.get('transaction_id');
      
      document.getElementById('success-page').style.display = 'block';
      document.getElementById('success-order-id').textContent = orderId || 'N/A';
      document.getElementById('success-transaction-id').textContent = transactionId || 'N/A';
      document.getElementById('success-time').textContent = new Date().toLocaleString('vi-VN');
      
      document.title = 'Thanh toán thành công';
    }
    
    function showFailedPage(urlParams) {
      const orderId = urlParams.get('order_id');
      const error = urlParams.get('error');
      
      document.getElementById('failed-page').style.display = 'block';
      document.getElementById('failed-order-id').textContent = orderId || 'N/A';
      document.getElementById('failed-error').textContent = error || 'Lỗi không xác định';
      document.getElementById('failed-time').textContent = new Date().toLocaleString('vi-VN');
      
      document.title = 'Thanh toán thất bại';
    }
  </script>
</body>
</html>